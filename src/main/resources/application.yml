server:
  port: ${PORT:8080}

spring:
  application:
    name: jcroft
logging:
  level:
    com.hazelcast: DEBUG
    org.hibernate.engine.jdbc.env.internal.LobCreatorBuilderImpl: ERROR
org.hibernate: DEBUG