package de.agdsn.jcroft.database.model.enums;

public enum ActorType {

    /**
    * if actor is a normal user
    */
    USER,

    /**
    * if actor is a service (e.q. a server) which uses our REST api
    */
    SERVICE

}
